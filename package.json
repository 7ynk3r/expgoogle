{
  "name": "expgoogle",
  "version": "0.0.0",
  "description": "Hello Expo!",
  "author": null,
  "private": true,
  "main": "main.js",
  "dependencies": {
    "expo": "16.0.0",
    "react": "16.0.0-alpha.6",
    "react-native": "https://github.com/exponent/react-native/archive/sdk-16.0.0.tar.gz"
  },
  "scripts": {
    "build:android": "rm -rf build && exp ba && gulp downloadBuild",
    "clean:start": "rm -rf node_modules/ .expo/ temp-ios-directory/ && npm cache clean && npm i && exp start -a"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-bump": "^2.7.0",
    "gulp-download": "0.0.1",
    "gulp-json-editor": "^2.2.1"
  }
}
